# Continue Platform Pack

- Put `sdd_spec.prompt` in your prompts folder.
- Register it as a slash command (e.g. `/sdd`).
- Use the command whenever you want Continue to operate in full Spec-Driven mode.
