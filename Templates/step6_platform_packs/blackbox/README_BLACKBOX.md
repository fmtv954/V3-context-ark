# Blackbox Platform Pack

- Use Prompt Caching to store the spec once (AGENTS + key docs).
- Subsequent calls should reference the cache instead of resending everything.
- This keeps costs down while preserving Spec-Driven behavior.
