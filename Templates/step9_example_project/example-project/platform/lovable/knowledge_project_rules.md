# Lovable Knowledge – Quantum Voice AI

Core rules:

- App purpose: QR-to-call voice lead capture.
- MVP: Single-tenant, simple Asana + email integration.
- Use the existing design system (Tailwind + shadcn/ui).

When generating pages:

- Preserve the main call-to-action: "Scan, Talk, Convert".
- Avoid adding complex features not described in the docs.
