# System – Runtime Flow Spec (Summary)

This file summarizes how this example project would be generated
by Context Architect:

1. Onboarding Intake → initial extraction.
2. Kernel Build → `project_kernel.json`.
3. Doc Generation → this `docs/` set.
4. Platform Packs → files in `platform/`.
5. Visual Maps → `docs/76_system_maps.md`.
6. Export & Handoff → this folder as a zip.

For the full spec, see the Step 8 runtime_flow_spec in your toolkit.
