# 83 – Project Kernel & Context Config Specification

> Defines which docs, files, and settings make up the “brain” for this project.
> Audience: AI platform glue code, infra, engineers wiring context into tools.

## 1. Kernel Concept

- **What the “project kernel” is in this system:**
  - {{kernel_concept}}

## 2. Core Inputs

- **Docs that MUST be included in the kernel (IDs from this library):**
  - {{kernel_core_doc_ids}}

## 3. Optional Inputs

- **Docs that are optional / tier-specific:**
  - {{kernel_optional_doc_ids}}

## 4. Platform Mapping

- **How the kernel maps into each IDE/tool (files, knowledge uploads, etc.):**
  - {{kernel_platform_mapping}}

## 5. Notes for AI Tools

- Use this spec when deciding what to load into context for a given task.
